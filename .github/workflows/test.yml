name: Action Tests
on:
  push:

jobs:
  ubuntu:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
		os: [ ubuntu-24.04, ubuntu-26.04, ubuntu-24.04-arm, ubuntu-26.04-arm, windows-latest, windows-11-arm, windows-11-vs2026-arm, macos-latest ]

    steps:
      - name: Install prerequisites
        run: |
          which vcpkg
          vcpkg --version
          echo "VCPKG_INSTALLATION_ROOT: [$VCPKG_INSTALLATION_ROOT]"
          vcpkg install sqlite3
