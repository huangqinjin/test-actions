name: Install

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  Windows:
    runs-on: windows-11-arm
    steps:
      - name: Test Arch
        run: |
          vcpkg --version
          Write-Host "host arch env: $env:PROCESSOR_ARCHITECTURE"
          python --version
          python -c "import platform; print(platform.machine())"
